# Leave empty for a read-only local roadmap. Never put secrets in PUBLIC_ values.
PUBLIC_EDIT_API=
# Enable only when hosting on Canvas Drop with its backend configured.
PUBLIC_CANVAS_BACKEND=false
# Canonical deployment URL; optional for local development.
SITE_URL=http://localhost:4321
